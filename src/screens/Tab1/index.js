import { View, Text } from "react-native";
import React from "react";

/**
 * This is the first tab a user should see after they have logged in to the app.
 * @param {*} props
 * @returns a Tab1 Functional Component
 */
export default function Tab1(props) {
  return (
    <View>
      <Text>Tab1</Text>
    </View>
  );
}
